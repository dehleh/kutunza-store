# Platform Admin - Static Site Configuration
[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_22"]
nixpkgsArchive = "ba913eda2df8eb72147259189d55932012df6301"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve -s dist -l $PORT"
