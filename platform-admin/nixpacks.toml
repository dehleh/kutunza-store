# Platform Admin - Static Site Configuration
[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_22"]
nixpkgsArchive = "057f63b6dc1a2c67301286152eb5af20747a9cb4"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve -s dist -l $PORT"
