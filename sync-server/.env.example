# Environment variables for sync server

# PostgreSQL Database URL (Railway provides this)
# For production: Use the DATABASE_URL from your hosting provider
# For local dev: postgresql://kutunza:changeme@localhost:5433/kutunza_pos
DATABASE_URL="postgresql://username:password@host:port/database"

# Server configuration
PORT=5000
NODE_ENV=production

# CORS - Comma-separated list of allowed origins (NEVER use * in production!)
# Example: "https://yourdomain.com,https://app.yourdomain.com,https://pos.yourdomain.com"
ALLOWED_ORIGINS="https://yourdomain.com"

# Authentication - CRITICAL: Generate strong secrets for production
# Generate API_KEY: openssl rand -hex 32
# Generate JWT_SECRET: openssl rand -base64 64
API_KEY="CHANGE_THIS_GENERATE_WITH_openssl_rand_hex_32"
JWT_SECRET="CHANGE_THIS_GENERATE_WITH_openssl_rand_base64_64"

# Logging
LOG_LEVEL="info"
